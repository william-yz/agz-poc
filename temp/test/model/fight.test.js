const {attact} = require('../../src/model/fight')
const utils = require('../../src/utils')
const expect = require('chai').expect
describe('mahjong test', function () {

  beforeEach(function () {
    
  })
  it('init', function () {
    expect(mahjongs.length).to.be.eq(108)
  })
})