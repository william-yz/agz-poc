import React, { Component, PropTypes } from 'react'

class Column {
  constructor (config) {
    { this.title, this.dataIndex, this.key } = {title, dataIndex, key}
  }

  render () {
    return (
      <table>
      </table>
    )
  }
}
